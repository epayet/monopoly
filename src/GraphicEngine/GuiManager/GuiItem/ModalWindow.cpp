#include "ModalWindow.h"

ModalWindow::ModalWindow(sf::RenderWindow& window, int state, int windowSizeX, int windowSizeY, int sizex, int sizey, std::string content) 
                        : GuiItem(window, state, 0, 0, sizex, sizey)
{
//    _x = windowSizeX / 
}

void ModalWindow::Draw(int state)
{
    
}